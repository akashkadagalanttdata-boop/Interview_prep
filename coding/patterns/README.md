# Pattern write-ups

One file per pattern, named `<NN>-<pattern>.md`, from `templates/pattern.md`.

Written **from recall** after you've solved a few problems in the pattern — not before, and not
by copying a reference. The highest-value section is **The tell**: what in a problem statement
signals this pattern. Most interview failures are identification failures, not implementation
failures.
